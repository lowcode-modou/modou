export enum ColumnValueTypeEnum {
  text = 'text',
  select = 'select',
}

export enum ColumnAlignEnum {
  left = 'left',
  center = 'center',
  right = 'right',
}

export enum ColumnFixedEnum {
  left = 'left',
  right = 'right',
  false = '',
}
